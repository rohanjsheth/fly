[project]
name = "fly"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "neuprint-python>=0.6.3",
    "pyarrow>=25.0.1",
    "python-dotenv>=1.2.3",
    "scipy>=1.17.1",
]

[project.optional-dependencies]
# Only produce.py needs these; the connectome utils are numpy/scipy.
# Preinstalled on Colab, so no install step there.
nrf = [
    "torch>=2.2",
    "torchvision>=0.17",
]
